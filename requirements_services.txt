# Core dependencies for GitHub Issues Dashboard Services
Flask==3.0.0
requests==2.31.0
sqlite3
Werkzeug==3.0.1

# Optional dependencies (will be imported if available)
python-dotenv>=1.0.0  # For loading environment variables
pytz>=2023.3          # For timezone support

# Development dependencies (optional)
pytest>=7.4.0         # For testing
pytest-flask>=1.3.0   # For Flask testing
